name: vep-bench
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.10
  - pandas>=2.0
  - numpy>=1.24
  - pyarrow>=14.0
  - requests>=2.28
  - pytest>=7.0
  - pip
  - pip:
    - requests-cache>=1.0
